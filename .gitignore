# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Production
dist
out
build

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
webpack_error.log

# VS Code (User settings)
.vscode-test

# We explicitly want to commit .vscode/launch.json and .vscode/tasks.json, 
# so we do NOT ignore .vscode directory generally.
# If there were user-specific settings we wanted to ignore, we would list them specifically
# e.g. .vscode/settings.json if it contained local paths. 
# For now, keeping .vscode open.
